import sys
from ds_store import DSStore

print(sys.argv[0])

#with DSStore.open(’/Users/alastair/.DS_Store’, ’r+’) as d:
    # Position the icon for "foo.txt" at (128, 128) d[’foo.txt’][’Iloc’] = (128, 128)
    # Display the plists for this folder
    #print d[’.’][’bwsp’] print d[’.’][’icvp’]
